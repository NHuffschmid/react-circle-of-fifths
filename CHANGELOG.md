# CHANGELOG

## Version 0.1.0
- Initial version: extracted from the DEPINUS project
- `CircleOfFifths` component: pure SVG renderer, `language` and `accentColor` props
- `useCircleOfFifthsDetection` hook: two-window key detection (chord + passage mode)
- Uses `react-piano-keyboard` as peer dependency for note names and Skrjabin colors
